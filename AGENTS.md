# robust-iniwa theme consumer

This checkout consumes the standalone `robust-iniwa` theme. Theme implementation belongs in `D:/Git/hugo_theme_robust-iniwa`; do not edit or advance the submodule pointer from this parent. Read the parent `AGENTS.md`, theme README, and any requesting handoff before work.

Preserve parent-specific overrides, `Site.Params` compatibility, template escaping, DOMPurify sanitization, SRI, upstream divergence markers, and README change tables. Keep unrelated submodule work, generated output, secrets, dependencies, deployment, and publication protected.

Use the parent site's Hugo commands and `git diff --check` as applicable. User runtime model/effort belongs to configuration. Default to primary design, implementation, verification, and final acceptance at any task size. Delegate only when replacing primary work lowers expected total effort including handoff, communication, waiting, integration, verification, and corrections, or a named material risk or existing mandatory independent verification gate warrants it. Size or uncertainty alone is insufficient; routine direct work needs no per-task justification.

The current task may narrow standing permissions; it never widens submodule, publication, or exposure gates. For bounded personal work, make a minimal diff and useful normal-path check, then use the established authorized target/procedure, smoke normal use, and correct observed failures. Cheap checks are optional; no speculative suite or harness is required. Only the primary delegates; configured roles must be observable or primary/equivalent continues, with parent permissions/live overrides/read-only behavior binding. Stable self-review precedes named-risk review and later edits invalidate it. A second correction or two blocked/partial returns requires primary contract restatement and one selected writer.

Before implementation, decide delegation, then role and route: `small-primary` for direct work of any size, `bounded` for settled delegated work, `adaptive` for delegated material technical uncertainty, or `non-implementation`. Reclassify only after a material scope change or contract reset. The primary may reclaim any size when cheaper or delegation is unavailable, after child writes stop, ownership returns, and acceptance, boundaries, authority, environment, and evidence are reset. Preserve mandatory independent and multi-reviewer gates and required verification. Do not add research/checks, inspect changing candidates, or repeat liveness polling/rereads/state updates merely to fill waits; do useful owned work or await notifications, while responding to errors, inconsistent state, user steering, and host progress rules.
